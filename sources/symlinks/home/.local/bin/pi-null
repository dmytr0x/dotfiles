#!/bin/zsh

# Load the same environment used by an interactive terminal.
[[ -f "$HOME/.zprofile" ]] && source "$HOME/.zprofile"
[[ -f "$HOME/.zshrc" ]] && source "$HOME/.zshrc"

cd "$HOME/Work/ai/topics/null" || exit 1

pim
